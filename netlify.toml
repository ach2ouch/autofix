# Netlify configuration for Autofix AI

[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Optional: increase function timeout if analyses take longer
[functions."analyze"]
  timeout = 30
